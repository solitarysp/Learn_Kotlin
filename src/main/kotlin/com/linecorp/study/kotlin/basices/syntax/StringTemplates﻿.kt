package com.linecorp.study.kotlin.basices.syntax

fun main() {
    val a = 6
    val b = 2
    println("A is : $a b is $b")
    println("A * B is : ${a * b}")
    println("A / B is : ${a / b}")

    val s = "abc"
    println("Data s is ${s.replace("a","A")}")
}
